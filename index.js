import React from 'react';
import ReactDOM from 'react-dom/client';
import StreetWearSite from './App';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <StreetWearSite />
  </React.StrictMode>
);
